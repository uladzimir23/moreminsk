node_modules/**
.next/**
out/**

# Print stylesheets for PDF reports (pandoc → Chrome headless) live outside the
# site DS — fixed px sizing maps cleanly to A4 mm at print DPI, and !important
# overrides pandoc's default standalone-template styles. Different constraints,
# different rules.
docs/**

# Design-lab routes are visual sandboxes for proposing direction (ADR-009 era).
# Free hand with hex literals, px, gradients, !important — until a chosen variant
# is promoted to a DS-conformant component.
src/app/(preview)/**
