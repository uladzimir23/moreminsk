name = "moreminsk-booking-relay"
main = "worker.js"
compatibility_date = "2026-01-01"

# Optional — lock CORS to the production origin once the domain is live.
# [vars]
# ALLOW_ORIGIN = "https://moreminsk.by"

# Secrets (do NOT put here — set via `wrangler secret put`):
#   TELEGRAM_BOT_TOKEN   — from @BotFather
#   TELEGRAM_CHAT_ID     — target chat / group id (use @userinfobot or getUpdates)
