---
type: architecture
tags: [architecture, structure, fsd]
updated: 2026-04-16
---

# Структура проекта

## Дерево папок

```
moreminsk/
├── public/
│   ├── fleet/                       # Фото яхт
│   ├── gallery/                     # Фото с мероприятий
│   ├── og/                          # OG-картинки на страницы
│   └── favicon/
├── messages/                        # Переводы next-intl (.po или .json)
│   ├── ru.po
│   └── en.po                        # опционально
├── src/
│   ├── app/
│   │   ├── [locale]/
│   │   │   ├── layout.tsx           # html+body, global styles, JsonLd org
│   │   │   ├── page.tsx             # главная
│   │   │   ├── fleet/
│   │   │   │   ├── page.tsx         # /fleet (хаб)
│   │   │   │   └── [slug]/
│   │   │   │       └── page.tsx     # /fleet/eva
│   │   │   ├── services/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [slug]/
│   │   │   │       └── page.tsx
│   │   │   ├── ceny/page.tsx
│   │   │   ├── galereya/page.tsx
│   │   │   ├── otzyvy/page.tsx
│   │   │   ├── kontakty/page.tsx
│   │   │   ├── o-nas/page.tsx
│   │   │   ├── faq/page.tsx
│   │   │   └── legal/
│   │   │       ├── politika/page.tsx
│   │   │       └── oferta/page.tsx
│   │   ├── layout.tsx               # root, пассивный
│   │   ├── not-found.tsx
│   │   ├── sitemap.ts               # генерация sitemap.xml
│   │   ├── robots.ts                # генерация robots.txt
│   │   └── opengraph-image.tsx      # OG главной
│   │
│   ├── widgets/                     # Крупные UI-блоки страниц
│   │   ├── header/
│   │   ├── footer/
│   │   ├── hero/
│   │   ├── fleet-grid/
│   │   ├── services-grid/
│   │   ├── price-table/
│   │   ├── gallery/
│   │   ├── reviews/
│   │   ├── faq/
│   │   ├── cta-form/
│   │   ├── why-us/
│   │   └── contacts/
│   │
│   ├── features/                    # Интерактивные фичи
│   │   ├── lead-form/               # Форма заявки + валидация
│   │   ├── yacht-filter/            # Фильтр флота по параметрам
│   │   └── booking-cta/             # Кнопка бронирования / Telegram
│   │
│   ├── entities/                    # Бизнес-сущности + карточки
│   │   ├── yacht/
│   │   │   ├── ui/YachtCard.tsx
│   │   │   └── model/types.ts       # type Yacht
│   │   ├── service/
│   │   │   ├── ui/ServiceCard.tsx
│   │   │   └── model/types.ts
│   │   └── review/
│   │       ├── ui/ReviewCard.tsx
│   │       └── model/types.ts
│   │
│   └── shared/                      # Инфраструктура + атомарный UI
│       ├── ui/                      # Button, Input, Modal, Accordion...
│       ├── lib/
│       │   ├── seo.ts               # generatePageMetadata, JsonLd builders
│       │   ├── cn.ts                # className helper
│       │   └── format.ts            # price, phone, date
│       ├── i18n/
│       │   ├── config.ts
│       │   ├── request.ts
│       │   └── routing.ts
│       ├── content/                 # Контент-as-code
│       │   ├── yachts.ts
│       │   ├── services.ts
│       │   ├── reviews.ts
│       │   └── faq.ts
│       ├── hooks/
│       ├── config/
│       │   └── site.ts              # SITE_URL, бренд, контакты
│       └── styles/
│           ├── tokens/
│           │   ├── _colors.scss
│           │   ├── _typography.scss
│           │   ├── _spacing.scss
│           │   ├── _breakpoints.scss
│           │   └── _index.scss      # @forward all
│           ├── mixins/
│           │   ├── _responsive.scss
│           │   └── _typography.scss
│           └── globals.scss
│
├── scripts/                         # Node-скрипты (опционально)
│   └── generate-sitemap-preview.ts
├── .env.example
├── .eslintrc.mjs
├── .stylelintrc.json
├── bun.lock
├── next.config.ts                   # output: "export"
├── tsconfig.json
└── package.json
```

## Правила импорта (FSD-lite)

```
app → widgets → features → entities → shared
```

- Верхний слой может импортить нижний.
- Между «ветками» одного слоя — через shared.
- Widget не импортит другой widget напрямую (только через shared/ui или features).

## Именование файлов

- Компоненты: `PascalCase.tsx`
- Стили к компоненту: `PascalCase.module.scss` (рядом с компонентом)
- Утилиты/хуки: `kebab-case.ts`
- Типы: `types.ts` (внутри `model/`)
- Page-файлы: Next.js-конвенция (`page.tsx`, `layout.tsx`, `loading.tsx`, `not-found.tsx`)

## Алиасы (tsconfig paths)

```json
{
  "@/app/*": ["./src/app/*"],
  "@/widgets/*": ["./src/widgets/*"],
  "@/features/*": ["./src/features/*"],
  "@/entities/*": ["./src/entities/*"],
  "@/shared/*": ["./src/shared/*"]
}
```

## Связанные документы
- [[Architecture Overview]]
- [[Routing & i18n]]
- [[42 - ADR/ADR-003 FSD-lite архитектура]]
