---
type: ux-patterns
tags: [design, ux, conversion, mobile-first]
updated: 2026-04-17
---

# UX-паттерны

## Базовый принцип — Mobile-first

> Решение заказчика (2026-04-17): UI/UX-логика по образцу **neuro-center** и **wedding** (внутренние референсы), строго **mobile-first**.

**Что это значит на практике:**

1. **Стили пишем от mobile к desktop.** Базовые правила в `.module.scss` — для узких экранов; через `@include media('md')` / `media('lg')` добавляем расширения для планшета и десктопа. Никаких `max-width` медиа.
2. **Touch-target ≥ 44×44px** на всех интерактивах (Apple HIG / Material). Кнопки в хедере, иконки соцсетей, чекбоксы — все.
3. **Один колонок по умолчанию.** Сетки `grid-cols-2/3` появляются только с `md`+. На мобиле всё стекается вертикально.
4. **Hero-картинка сверху, текст снизу на mobile**, в desktop — текст слева, картинка справа (`order-2 / order-1` пара через flex/grid order).
5. **Шрифты — `clamp()` или responsive токены.** H1 не задаётся фиксированным `64px`, а `clamp(2rem, 5vw, 4.5rem)`.
6. **Spacing по токенам.** Никаких произвольных `padding: 47px` — только `--space-*` ladder из tokens.
7. **Изображения через `<picture>`** с разными `srcset` для mobile/tablet/desktop (см. skill `nextjs-static-export` → `<Image />`).
8. **Тестируем 360px шириной.** Если что-то ломается на iPhone SE / маленьком Android — баг.

## Референс-паттерны (из neuro-center / wedding)

### Фиксированный header со scroll-стейтом
- При scroll > 20px добавляется `backdrop-filter: blur` + полупрозрачный белый фон + нижний бордер.
- На mobile — бургер с анимацией трёх линий → крестик (rotate + opacity).
- Mobile-меню раскрывается под header'ом (height auto + opacity).
- Логотип всегда виден; текст рядом с лого скрывается до `sm:`.

### Hero с двумя колонками (на десктопе) и одной (на мобиле)
- Слева: badge с пульсирующей точкой → H1 → лид-абзац → 2 CTA (primary + ghost) → 3 квик-стата (число / лейбл).
- Справа: фото в `aspect-[4/5]` с фигурным радиусом и floating-карточкой поверх (idle bobbing).
- На mobile фото идёт **первым** (`order-1`), текст — вторым (`order-2`). Это критично для эмоционального эффекта.
- Анимации — `framer-motion` `fadeUp` со стаггером 0.12s между элементами; easing `[0.22, 1, 0.36, 1]`.

### Section-rhythm
Секции страницы идут одна под другой с одинаковым вертикальным ритмом:
- mobile: `padding-block: 4rem` (~64px)
- desktop: `padding-block: 6rem` (~96px)

Каждая секция — самодостаточный widget с собственным фоном/градиентом, без визуальных «швов».

### Soft pastel gradients + glow-pseudoelements
Фон секций — мягкие пастельные градиенты с blurred-кругами в углах (`blur(80px)`, opacity 0.5–0.6). Не отвлекают, но создают атмосферу.

> Для moreminsk вместо «розовый/голубой» — морская палитра: глубокий синий, бирюзовый, песочно-белый, акценты — закатный коралл.

### Smooth-scroll к якорям
Хедер-навигация и кнопки CTA внутри страницы — через `element.scrollIntoView({ behavior: 'smooth' })`. Не через `<a href="#anchor">` (избегаем jump).

## Формула страницы услуги (по бризу заказчика)

## Формула страницы услуги (по бризу заказчика)

```
┌────────────────────────────────────────────────┐
│ HERO: лид + цена + кнопка + фото              │  ← первый экран
├────────────────────────────────────────────────┤
│ ТАБЛИЦА ЦЕН: пакеты/опции                      │
├────────────────────────────────────────────────┤
│ ГАЛЕРЕЯ: фото с реальных мероприятий           │
├────────────────────────────────────────────────┤
│ CTA-2: «Забронировать» + форма                 │
├────────────────────────────────────────────────┤
│ ОТЗЫВЫ: 3–5 карточек с фото клиентов          │
├────────────────────────────────────────────────┤
│ FAQ: 4–6 частых вопросов (JSON-LD FAQPage)    │
├────────────────────────────────────────────────┤
│ ФУТЕР: контакты, карта, мессенджеры           │
└────────────────────────────────────────────────┘
```

## Дополнительные mobile-паттерны (критично — 70%+ трафика с mobile)

### Sticky CTA-bar
Всегда виден внизу экрана на мобильном:
```
[🟢 WhatsApp] [📱 Telegram] [☎️ Позвонить]
```

### Forma в lightbox
Не открывается «вниз страницы», а в модалке — сохраняет контекст.

### Фото в swipe-галерее
Лёгкая галерея с touch-свайпом. Без тяжёлых lightbox-либ — свой на Embla Carousel или Swiper (выбрать).

### Clickable контакты
Телефон, адрес, email — кликабельные (`tel:`, `mailto:`, `https://maps...`).

## Hero — как делаем

**Anti-pattern (не делаем):**
- Огромная слайдер-карусель, забирающая весь экран
- «Назамабываемые впечатления...» в H1
- Кнопка «Узнать больше» без указания действия

**Наш паттерн:**
```
H1: <конкретный запрос>
Лид: 2–3 предложения по делу (с ценой!)
[Кнопка: «Свободные даты»] [Кнопка: «Позвонить»]
<Крупное фото или короткое видео>
```

## Карточка яхты

```
┌────────────────────────────┐
│   [Фото 16:9 с 4 превью]   │
├────────────────────────────┤
│  Яхта EVA                  │
│  Парусная · до 6 чел · 8м  │
│  от 150 BYN/час            │
│  [Подробнее] [Забронировать]│
└────────────────────────────┘
```

Ключевые элементы — вместимость, тип, длина, цена. Цена — всегда на карточке.

## Прайс-таблица

Три колонки: Час / Полдня / День / Ночь. Строки — каждая яхта. В ячейке — цена + примечание (если минимум 2 часа).

| Яхта | Час | Полдня (4ч) | День (8ч) | Ночь |
|---|---|---|---|---|
| EVA | 150 BYN | 500 BYN | 900 BYN | 400 BYN |
| ALFA | 150 BYN | 500 BYN | 900 BYN | 400 BYN |
| MARIO | 150 BYN | 500 BYN | 900 BYN | 400 BYN |
| BRAVO | 200 BYN | 700 BYN | 1200 BYN | 500 BYN |

*Цены — draft, финальные согласовать с заказчиком.*

## Отзыв — карточка

```
┌──────────────────────────────┐
│ [avatar] Анна К.   ★★★★★    │
│         15 марта 2026        │
├──────────────────────────────┤
│ «Отметили день рождения      │
│  на Alfa — всё супер...»    │
│                              │
│ [Мероприятие: Д.Р.] [Яхта:   │
│  ALFA]                       │
│                              │
│ [Источник: Instagram 🔗]     │
└──────────────────────────────┘
```

**Анти-стоки:** отзывы — с реальными именами (первая буква фамилии), датой, ссылкой на источник (Инстаграм), тегами (что/на чём). Без этого — не доверяют.

## FAQ — аккордеон

- Свёрнут по умолчанию
- JSON-LD FAQPage — для богатых сниппетов Google
- 4–6 вопросов на страницу услуги; полная версия на /faq

## Формы — правила

1. **Минимум полей:** имя + телефон/telegram + дата + комментарий. Email опционально.
2. **Валидация realtime** (на blur, не на каждое нажатие).
3. **Честные ошибки:** «Телефон начинается с +375», не «Invalid format».
4. **Success state** в той же модалке: «Спасибо! Менеджер ответит в Telegram в течение 30 мин.»
5. **Без капчи для пользователя** — honeypot или invisible reCAPTCHA.

## CTA — лексикон

| ✅ Используем | ❌ Избегаем |
|---|---|
| Свободные даты | Узнать больше |
| Забронировать | Оставить заявку |
| Написать в Telegram | Связаться с нами |
| Позвонить | Заказать обратный звонок |
| Посмотреть яхту | Подробнее |

## Доверие — что добавить

1. Логотипы партнёров (фотографы, кейтеринг, декораторы) — если есть
2. «Работаем с {year}» в футере
3. Юр. адрес в футере
4. Лицензия/страховка (если есть — копия в `/legal`)
5. Фото команды/капитанов на `/o-nas`
6. Счётчик: «За 2025 год — X мероприятий» (если есть цифры)

## Связанные документы
- [[Design System]]
- [[Медиа-стратегия]]
- [[../20 - Market/Позиционирование]]
